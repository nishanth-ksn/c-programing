//write a cprogram to multiply teo floating point numbers and printing the output both in int and floating point repressentations.
#include<stdio.h>

int main()
{
	float a=2.5,b=5.5;
	int c;
	
	c=a*b;
	printf("multiply value in integer :%d",c);
	printf("\nmultiply value in floarting point:%f",a*b);
	return 0;
}
